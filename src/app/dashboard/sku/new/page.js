import { SkuCreateView } from '../../../../sections/sku/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Sku',
};

export default function SkuCreatePage() {
  return <SkuCreateView />;
}
