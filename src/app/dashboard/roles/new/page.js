import { RolesCreateView } from '../../../../sections/roles/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Roles',
};

export default function RolesCreatePage() {
  return <RolesCreateView />;
}
