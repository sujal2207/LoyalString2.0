import { PacketCreateView } from '../../../../sections/packet/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Packet',
};

export default function PacketCreatePage() {
  return <PacketCreateView />;
}
