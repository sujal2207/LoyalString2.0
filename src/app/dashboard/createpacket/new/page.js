import { CreatePacketCreateView } from '../../../../sections/create-packet/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new CreatePacket',
};

export default function CreatePacketCreatePage() {
  return <CreatePacketCreateView />;
}
