import { CollectionCreateView } from '../../../../sections/collection/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Collection',
};

export default function CollectionCreatePage() {
  return <CollectionCreateView />;
}
