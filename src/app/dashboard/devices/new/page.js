import { DevicesCreateView } from '../../../../sections/devices/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Devices',
};

export default function DevicesCreatePage() {
  return <DevicesCreateView />;
}
