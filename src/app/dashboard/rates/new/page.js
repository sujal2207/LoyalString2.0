import { RatesCreateView } from '../../../../sections/rates/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Rates',
};

export default function RatesCreatePage() {
  return <RatesCreateView />;
}
