import { CounterCreateView } from '../../../../sections/counter/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Counter',
};

export default function CounterCreatePage() {
  return <CounterCreateView />;
}
