import { CategoryCreateView } from '../../../../sections/category/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Category',
};

export default function CategoryCreatePage() {
  return <CategoryCreateView />;
}
