import { TaxCreateView } from '../../../../sections/tax/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Tax',
};

export default function TaxCreatePage() {
  return <TaxCreateView />;
}
